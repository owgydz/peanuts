# Quickstart

Create a file called `hello.nut`:
``` 
nut main():
    print("Hello, Peanuts!")

main()
```
Run with `peanuts run hello.nut`

Peanuts supports:

- Functions (`nut`)
- Variables (`jar`)
- Conditionals (`crunch`)
- Loops (`chew`)
- Imports